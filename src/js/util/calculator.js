/*global window*/
(function (window) {
    'use strict';

    window.Calculator = function () {
    };
    window.Calculator.prototype.multiply = function (x, y) {
        return x * y;
    };

})(window);